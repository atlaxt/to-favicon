# @atlaxt/to-favicon

Convert any PNG to a multi-size `favicon.ico` with a single command.

```bash
npx @atlaxt/to-favicon <path-to-png>
```

![demo](assets/record.gif)

`favicon.ico` is saved to the `public/` folder if it exists, otherwise to the current directory.
